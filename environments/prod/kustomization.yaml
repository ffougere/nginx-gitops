apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - namespace.yaml
  - ../../base

nameSuffix: -prod

images:
  - name: nginx
    newName: nginx
    newTag: "1.28.2"

replicas:
  - name: nginx
    count: 3

commonLabels:
  environment: prod
