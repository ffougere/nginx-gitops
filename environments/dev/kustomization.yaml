apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - namespace.yaml
  - ../../base

nameSuffix: -dev

images:
  - name: nginx
    newName: nginx
    newTag: "1.28.0"

replicas:
  - name: nginx
    count: 1

commonLabels:
  environment: dev
